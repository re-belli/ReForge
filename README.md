# ReForge
## AI-assisted semantic source reconstruction from binaries for vulnerability discovery.

Still working on the AI portion. It be easy to hook into online model but I would like to see if offline is possible via training. I also have to include CodeQL logic, still building the rules.
